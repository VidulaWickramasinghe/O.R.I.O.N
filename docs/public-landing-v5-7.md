# O.R.I.O.N. v5.7 — Public Demo Website + Landing Page Export

The `/public-demo` route is presentation-only and static-export compatible. It does not publish, push, expose secrets, or bypass approvals.
