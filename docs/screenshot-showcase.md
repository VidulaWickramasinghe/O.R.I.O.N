# O.R.I.O.N. Screenshot Showcase

## Mission Control dashboard

The existing dashboard preview remains available at:

```text
assets/screenshots/aurora-dashboard.png
```

New v6.7 screenshots are intentionally not committed because generated binary
media is excluded from source changes. To capture an updated local preview after
building the frontend, serve `frontend/out/` and use a browser screenshot tool at
a 1,440 × 1,000 desktop viewport.
