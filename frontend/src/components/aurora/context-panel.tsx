"use client";
import { Activity, ShieldCheck, X } from "lucide-react"; import { useUiStore } from "@/store/ui-store"; import { AuroraCard } from "./data-display/AuroraCard"; import { StatusBadge } from "./data-display/StatusBadge";
export function ContextPanel(){const open=useUiStore(s=>s.contextOpen);const close=useUiStore(s=>s.setContextOpen);return <aside aria-label="Mission context" className={`${open?"translate-x-0":"translate-x-full xl:hidden"} fixed inset-y-0 right-0 z-40 w-[min(360px,calc(100vw-1rem))] border-l border-white/10 bg-slate-950/95 p-4 pt-20 shadow-2xl transition-transform xl:static xl:h-screen xl:w-[340px] xl:translate-x-0 xl:pt-4 ${open?"xl:block":"xl:hidden"}`}><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-cyan-300">Context rail</p><h2 className="mt-1 text-lg font-semibold">Mission awareness</h2></div><button aria-label="Close context rail" onClick={()=>close(false)} className="aurora-icon-button"><X size={17}/></button></div><div className="mt-5 space-y-3 overflow-y-auto"><AuroraCard title="Current selection" description="Select a mission, agent, memory, approval, or tool event to inspect its live context."><StatusBadge status="Unavailable"/></AuroraCard><AuroraCard title="Approval posture" action={<ShieldCheck size={18} className="text-amber-300"/>}><p className="text-sm leading-6 text-slate-400">Risky operations continue through the existing approval and tool-permission systems.</p></AuroraCard><AuroraCard title="Recent context" action={<Activity size={18} className="text-cyan-300"/>}><p className="text-sm leading-6 text-slate-400">No operational context has been selected in this session.</p></AuroraCard></div></aside>}
