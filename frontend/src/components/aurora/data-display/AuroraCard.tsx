import type { HTMLAttributes, ReactNode } from "react";
import { cn } from "@/lib/utils";
export function AuroraCard({ title, description, action, className, children, ...props }: HTMLAttributes<HTMLElement> & { title?: string; description?: string; action?: ReactNode }) { return <section className={cn("min-w-0 rounded-2xl border border-[var(--aurora-border)] bg-[var(--aurora-panel)] p-4 shadow-[var(--aurora-shadow)] sm:p-5",className)} {...props}>{(title||description||action)&&<header className="mb-4 flex min-w-0 items-start justify-between gap-3"><div className="min-w-0">{title&&<h2 className="break-words text-base font-semibold text-white">{title}</h2>}{description&&<p className="mt-1 break-words text-sm leading-6 text-slate-400">{description}</p>}</div>{action}</header>}{children}</section>; }
