"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { Home, MessageSquare, Briefcase, Bot, Menu } from "lucide-react"; import { useUiStore } from "@/store/ui-store"; import { cn } from "@/lib/utils";
const items=[{href:"/",label:"Home",icon:Home},{href:"/assistant",label:"Assistant",icon:MessageSquare},{href:"/missions",label:"Missions",icon:Briefcase},{href:"/agents",label:"Agents",icon:Bot}];
export function MobileNavigation(){const path=usePathname();const setMobileNavOpen=useUiStore(s=>s.setMobileNavOpen);return <nav aria-label="Primary mobile navigation" className="fixed inset-x-0 bottom-0 z-50 grid h-16 grid-cols-5 border-t border-white/10 bg-slate-950/95 px-[max(0.5rem,env(safe-area-inset-left))] pb-[env(safe-area-inset-bottom)] backdrop-blur-xl lg:hidden">{items.map(item=>{const Icon=item.icon;return <Link key={item.href} href={item.href} className={cn("flex flex-col items-center justify-center gap-1 text-[11px]",path===item.href?"text-cyan-300":"text-slate-400")}><Icon size={18}/>{item.label}</Link>})}<button aria-label="Open navigation menu" onClick={()=>setMobileNavOpen(true)} className="flex flex-col items-center justify-center gap-1 text-[11px] text-slate-400"><Menu size={18}/>More</button></nav>}
