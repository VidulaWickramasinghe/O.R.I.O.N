import { AURORA_DASHBOARD_PRESETS } from "@/lib/panelRegistry"; import type { AuroraDashboardViewId } from "@/types/panels"; import { GlassPanel } from "@/components/aurora/glass-panel";
export function DashboardViewSelectorPanel({activeDashboardView,onApplyView}:{activeDashboardView:AuroraDashboardViewId;onApplyView:(id:AuroraDashboardViewId)=>void}){return <GlassPanel className="border-cyan-400/20 bg-white/[0.06] p-5"><h2 className="text-xl font-bold text-white">Dashboard Views</h2><p className="text-sm text-slate-400">Switch Aurora OS between focused workspace views</p><div className="mt-4 grid gap-2">{AURORA_DASHBOARD_PRESETS.map(p=><button key={p.id} onClick={()=>onApplyView(p.id)} className={`rounded-xl border p-3 text-left ${p.id===activeDashboardView?"border-cyan-400/40 bg-cyan-500/10":"border-white/10 bg-white/5"}`}><b>{p.icon} {p.name}</b><p className="mt-1 text-xs text-slate-400">{p.description}</p></button>)}</div><p className="mt-4 text-xs text-slate-500">Safety: views only change local panel visibility and ordering.</p></GlassPanel>}
