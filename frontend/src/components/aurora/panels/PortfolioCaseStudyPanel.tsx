import { ORION_PORTFOLIO_FEATURES, ORION_PORTFOLIO_MILESTONES } from "@/lib/portfolioRegistry";
import { GlassPanel } from "@/components/aurora/ui/GlassPanel";
export function PortfolioCaseStudyPanel() { return <GlassPanel><div className="space-y-4 p-4"><h2 className="font-bold text-cyan-200">Portfolio Case Study · v5.2</h2><p className="text-sm text-slate-300">O.R.I.O.N. is a local-first AI desktop agent built around safe agentic execution and Aurora OS, a modular command center for tools, memory, security, and release readiness.</p><pre className="rounded-xl bg-black/40 p-3 text-xs text-slate-300">Aurora OS → API service layer → FastAPI → tool registry → plugin permissions → approvals → audit history</pre><h3 className="text-sm font-semibold text-cyan-300">Feature Highlights</h3>{ORION_PORTFOLIO_FEATURES.map(item => <div key={item.title} className="rounded-xl border border-white/10 p-3"><b>{item.title}</b><p className="text-xs text-slate-400">{item.description}</p></div>)}<h3 className="text-sm font-semibold text-cyan-300">Release Journey</h3>{ORION_PORTFOLIO_MILESTONES.map(item => <div key={item.version} className="text-xs text-slate-400"><b className="text-emerald-200">{item.version}</b> — {item.title}: {item.description}</div>)}</div></GlassPanel>; }
