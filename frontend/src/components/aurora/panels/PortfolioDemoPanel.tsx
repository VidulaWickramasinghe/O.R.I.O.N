import { GlassPanel } from "@/components/aurora/ui/GlassPanel";
const steps = ["Open Aurora OS and introduce its local-first design.", "Show Dashboard Intelligence and the current readiness state.", "Switch dashboard views and explain that visibility does not change permissions.", "Show Security Policy, Plugin System, permission enforcement, and audit history.", "Run Quality Gate and review the local verification report.", "Generate local public-release assets without automatic publishing.", "Close with the portfolio case study and architecture story."];
export function PortfolioDemoPanel() { return <GlassPanel><div className="space-y-3 p-4"><h2 className="font-bold text-cyan-200">Portfolio Demo · v5.2</h2>{steps.map((step, index) => <div key={step} className="rounded-xl border border-white/10 p-3 text-sm text-slate-300"><b className="text-cyan-200">{String(index + 1).padStart(2, "0")}</b> {step}</div>)}<p className="text-xs text-slate-500">Presentation-only; this narrative triggers no external publishing.</p></div></GlassPanel>; }
