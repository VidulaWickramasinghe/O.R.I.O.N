import type { PortfolioShowcaseResult } from "@/types/orion";
import { GlassPanel } from "@/components/aurora/ui/GlassPanel";
type Props={result:PortfolioShowcaseResult|null;loading:boolean;onLoad:()=>void;onSave:()=>void};
export function PortfolioShowcaseStatusPanel({result,loading,onLoad,onSave}:Props){return <GlassPanel><div className="space-y-3 p-4"><h2 className="font-bold text-cyan-200">Portfolio Showcase Status · v6.5</h2><div className="grid grid-cols-2 gap-2"><button onClick={onLoad} disabled={loading} className="rounded-xl bg-cyan-300 p-2 font-bold text-slate-950">Check Showcase</button><button onClick={onSave} disabled={loading} className="rounded-xl border border-emerald-400/30 p-2 text-emerald-200">Save Report</button></div>{result && <><p className="text-slate-200">{result.status}: {result.existing_count}/{result.expected_count} screenshots available</p><pre className="max-h-64 overflow-auto text-xs text-slate-400">{result.report}</pre></>}<p className="text-xs text-slate-500">Presentation-only; no external publishing.</p></div></GlassPanel>}
