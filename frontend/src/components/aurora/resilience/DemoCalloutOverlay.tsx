import type { DemoWalkthroughState } from "@/types/demo";
import { ORION_DEMO_WALKTHROUGH_STEPS } from "@/lib/demoWalkthroughRegistry";
type Props = { demoWalkthroughState: DemoWalkthroughState; onNext: () => void; onStop: () => void; };
export function DemoCalloutOverlay({ demoWalkthroughState, onNext, onStop }: Props) { if (!demoWalkthroughState.enabled) return null; const currentStep = ORION_DEMO_WALKTHROUGH_STEPS[demoWalkthroughState.currentStepIndex] || ORION_DEMO_WALKTHROUGH_STEPS[0]; return <div className="fixed bottom-6 right-6 z-50 max-w-md rounded-[2rem] border border-cyan-400/30 bg-slate-950/90 p-5 shadow-2xl shadow-cyan-950/40 backdrop-blur-xl"><div className="flex items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-[0.25em] text-cyan-300">Guided Demo</p><h3 className="mt-2 text-lg font-black text-slate-100">{currentStep.stepNumber}. {currentStep.title}</h3></div><button onClick={onStop} className="rounded-full border border-white/10 px-3 py-1 text-xs text-slate-400 hover:bg-white/10">Close</button></div><p className="mt-3 text-sm leading-6 text-slate-300">{currentStep.callout}</p><div className="mt-4 flex items-center justify-between gap-3"><span className="text-xs text-slate-500">Step {demoWalkthroughState.currentStepIndex + 1} / {ORION_DEMO_WALKTHROUGH_STEPS.length}</span><button onClick={onNext} className="rounded-xl bg-cyan-300 px-4 py-2 text-xs font-bold text-slate-950 hover:bg-cyan-200">Next Step</button></div></div>; }
