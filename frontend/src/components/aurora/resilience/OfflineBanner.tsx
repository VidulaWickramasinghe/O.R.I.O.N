export function OfflineBanner({online,lastCheckedAt,lastError,onRetry}:{online:boolean;lastCheckedAt:string;lastError:string;onRetry:()=>void}){if(online)return null;return <section className="rounded-[2rem] border border-red-400/30 bg-red-500/10 p-5"><p className="text-xs uppercase text-red-200">Backend Offline</p><h2 className="mt-2 text-xl font-black text-red-100">O.R.I.O.N. backend connection is unavailable</h2><p className="mt-2 text-sm text-red-100/80">Aurora OS remains running, but live modules may show stale data.</p>{lastError&&<p className="mt-2 text-xs text-red-200">Last error: {lastError}</p>}{lastCheckedAt&&<p className="text-xs text-red-200/60">Last checked: {lastCheckedAt}</p>}<button onClick={onRetry} className="mt-4 rounded-xl bg-red-200 px-4 py-2 text-sm font-bold text-red-950">Retry Connection</button></section>}
