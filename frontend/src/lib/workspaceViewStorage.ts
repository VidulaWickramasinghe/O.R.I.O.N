import type { AuroraDashboardViewId } from "@/types/panels"; import type { WorkspaceViewPreference } from "@/types/workspaceViews";
const KEY="orion_workspace_view_preferences_v4_7"; const load=():WorkspaceViewPreference[]=>{if(typeof window==="undefined")return[];try{return JSON.parse(window.localStorage.getItem(KEY)||"[]")}catch{return[]}}; const save=(x:WorkspaceViewPreference[])=>{if(typeof window!=="undefined")window.localStorage.setItem(KEY,JSON.stringify(x))}; export const getWorkspaceViewPreference=(id:number)=>load().find(x=>x.workspaceId===id)||null; export const setWorkspaceViewPreference=(workspaceId:number,workspaceName:string,preferredDashboardView:AuroraDashboardViewId)=>{const all=load().filter(x=>x.workspaceId!==workspaceId);all.push({workspaceId,workspaceName,preferredDashboardView,updatedAt:new Date().toISOString()});save(all)};export const listWorkspaceViewPreferences=()=>load();
